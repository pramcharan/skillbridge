# SkillBridge – Unnecessary & Redundant Files Analysis

> [!IMPORTANT]
> All items below are **safe to remove** based on cross-referencing actual imports, controller wiring, and service usage across the entire codebase. No production-critical code is affected.

---

## 🔴 Category 1 – Duplicate / Shadowed DTOs (Highest Priority)

These files are **completely unused** — the codebase uses a differently-named equivalent instead.

| Unused File | Active Replacement | Evidence |
|---|---|---|
| [JobRequest.java](file:///c:/skillbridge/skillbridge/src/main/java/com/skillbridge/dto/request/JobRequest.java) | `PostJobRequest.java` | `JobController` and `JobService` both import `PostJobRequest`. `JobRequest` is never imported anywhere. |
| [ProposalRequest.java](file:///c:/skillbridge/skillbridge/src/main/java/com/skillbridge/dto/request/ProposalRequest.java) | `SubmitProposalRequest.java` | `ProposalController` and `ProposalService` use `SubmitProposalRequest`. `ProposalRequest` is never imported. |
| [ReviewRequest.java](file:///c:/skillbridge/skillbridge/src/main/java/com/skillbridge/dto/request/ReviewRequest.java) | `SubmitReviewRequest.java` | `ReviewController` and `ReviewService` use `SubmitReviewRequest`. `ReviewRequest` is never imported. |
| [RevisionRequest.java](file:///c:/skillbridge/skillbridge/src/main/java/com/skillbridge/dto/request/RevisionRequest.java) | `RevisionRequestDTO.java` | `RevisionController` and `RevisionService` both use `RevisionRequestDTO`. `RevisionRequest` is never imported. Also note `RevisionRequest` redundantly annotates `@Data @Getter @Setter` all together — poor code quality. |

**Action:** Delete all 4 files.

---

## 🔴 Category 2 – Duplicate Repository (Highest Priority)

| Unused File | Active Replacement | Evidence |
|---|---|---|
| [DisputeTicketRepository.java](file:///c:/skillbridge/skillbridge/src/main/java/com/skillbridge/repository/DisputeTicketRepository.java) | `DisputeRepository.java` | `DisputeService` only injects `DisputeRepository`. `DisputeTicketRepository` is **never injected or imported** anywhere — it's a leftover stub from an earlier refactor. |

**Action:** Delete `DisputeTicketRepository.java`.

---

## 🔴 Category 3 – Orphaned Local File Storage Service

| Unused File | Evidence |
|---|---|
| [FileStorageService.java](file:///c:/skillbridge/skillbridge/src/main/java/com/skillbridge/service/FileStorageService.java) | Saves files to local disk (`/uploads/`). **Nothing in the codebase injects or calls it** — zero references outside the class itself. All file uploads in production go through `FileUploadService` (Cloudinary). This service was superseded when Cloudinary integration was added. |

**Action:** Delete `FileStorageService.java`.

---

## 🟡 Category 4 – Generated / Auto-created Build Artifacts

These directories are **auto-regenerated** on every build and should not be in the project (and ideally should be in `.gitignore`).

| Directory/File | Size | Why Remove |
|---|---|---|
| [target/](file:///c:/skillbridge/skillbridge/target) | Large | Maven build output. Auto-generated by `mvn package`. Should never be committed. |
| [playwright-report/](file:///c:/skillbridge/skillbridge/playwright-report) | ~550 KB | Auto-generated Playwright HTML report from the last test run. Re-created every time tests run. |
| [test-results/](file:///c:/skillbridge/skillbridge/test-results) | Multiple dirs | Auto-generated Playwright test artifacts (screenshots, traces). Re-created on every run. |
| [node_modules/](file:///c:/skillbridge/skillbridge/node_modules) | Large | NPM dependency cache. Restored with `npm install`. Should never be committed. |

**Action:** Delete these directories and confirm they are listed in `.gitignore`.

---

## 🟡 Category 5 – Empty / Placeholder Directory

| Directory | Issue |
|---|---|
| [src/main/resources/static/lib/](file:///c:/skillbridge/skillbridge/src/main/resources/static/lib) | Completely **empty directory** — no files inside. |

**Action:** Delete the empty `lib/` directory.

---

## 🟡 Category 6 – Redundant Documentation Files

| File | Issue |
|---|---|
| [HELP.md](file:///c:/skillbridge/skillbridge/HELP.md) | Default Spring Initializr boilerplate — generic Spring Boot starter links, no project-specific content. Adds no value. |
| [project_overview.md](file:///c:/skillbridge/skillbridge/project_overview.md) | Generic overview stub. Your actual documentation is in `SKILLBRIDGE_DOCUMENTATION.md` (63KB at the root) and `CLAUDE.md`. This file is redundant. |
| [c:\skillbridge\SKILLBRIDGE_DOCUMENTATION.md](file:///c:/skillbridge/SKILLBRIDGE_DOCUMENTATION.md) | Sits in the root `c:\skillbridge\` parent folder (not inside the project). Consider moving it into `skillbridge/` or removing if `CLAUDE.md` covers it. |

**Action:** Delete `HELP.md` and `project_overview.md`. Evaluate the root-level `SKILLBRIDGE_DOCUMENTATION.md`.

---

## 🟢 Category 7 – Test Infrastructure (Optional / Keep If Using)

| File/Directory | Notes |
|---|---|
| [tests/](file:///c:/skillbridge/skillbridge/tests) | Contains Playwright E2E tests (`skillbridge.spec.js`, `qa-audit.spec.js`). Keep if actively used, but the test-results show many failing runs — consider if these are maintained. |
| [reports/api-report.html](file:///c:/skillbridge/skillbridge/reports/api-report.html) | 400KB generated HTML API test report. Like `playwright-report/`, this should be generated fresh on demand, not committed. |
| [postman/SkillBridge.postman_collection.json](file:///c:/skillbridge/skillbridge/postman/SkillBridge.postman_collection.json) | Keep if used for API testing during development. Otherwise it can be removed or moved to a shared team location. |

---

## 📋 Summary – Safe Deletions

### Java Source Files (6 files)
```
src/main/java/com/skillbridge/dto/request/JobRequest.java          ← never used
src/main/java/com/skillbridge/dto/request/ProposalRequest.java     ← never used  
src/main/java/com/skillbridge/dto/request/ReviewRequest.java       ← never used
src/main/java/com/skillbridge/dto/request/RevisionRequest.java     ← never used
src/main/java/com/skillbridge/repository/DisputeTicketRepository.java ← never injected
src/main/java/com/skillbridge/service/FileStorageService.java      ← never injected
```

### Directories & Generated Files
```
skillbridge/target/               ← Maven build output (regenerated)
skillbridge/playwright-report/    ← Auto-generated test HTML
skillbridge/test-results/         ← Auto-generated test artifacts
skillbridge/node_modules/         ← NPM cache (restored with npm install)
skillbridge/reports/              ← Generated API report
src/main/resources/static/lib/    ← Empty directory
```

### Markdown Documentation
```
skillbridge/HELP.md               ← Spring Initializr boilerplate
skillbridge/project_overview.md   ← Redundant stub
```

---

> [!TIP]
> After deleting the Java files, rebuild the project with `mvn clean compile` to confirm zero compilation errors. The project should build cleanly since none of these files are imported anywhere.
